<Window x:Class="DynaOrchestrator.Desktop.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="DynaOrchestrator 控制台" Height="700" Width="1100">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="200"/>
        </Grid.RowDefinitions>

        <GroupBox Grid.Row="0" Header="任务控制与全局配置" Margin="0,0,0,10">
            <StackPanel Orientation="Vertical" Margin="5">
                <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                    <TextBlock Text="工作区目录 (RootDir):" VerticalAlignment="Center" Margin="0,0,5,0"/>
                    <TextBox Text="{Binding WorkspaceRootDir, UpdateSourceTrigger=PropertyChanged}" Width="150" VerticalContentAlignment="Center" Margin="0,0,20,0" ToolTip="相对于程序运行目录"/>

                    <TextBlock Text="CSV 路径 (CasesCsv):" VerticalAlignment="Center" Margin="0,0,5,0"/>
                    <TextBox Text="{Binding CasesCsvPath, UpdateSourceTrigger=PropertyChanged}" Width="200" VerticalContentAlignment="Center" Margin="0,0,20,0" ToolTip="相对于工作区目录"/>
                </StackPanel>

                <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                    <TextBlock Text="执行阶段开关:" VerticalAlignment="Center" FontWeight="Bold" Margin="0,0,10,0"/>
                    <CheckBox Content="1. 前处理 (网格生成)" IsChecked="{Binding EnablePreProcessing}" VerticalAlignment="Center" Margin="0,0,20,0" ToolTip="取消勾选将跳过基于STL生成网格的过程，直接使用已有K文件。"/>
                    <CheckBox Content="2. 求解器 (LS-DYNA)" IsChecked="{Binding EnableSimulation}" VerticalAlignment="Center" Margin="0,0,20,0" ToolTip="取消勾选将不启动DYNA计算。"/>
                    <CheckBox Content="3. 后处理 (特征提取与NPZ)" IsChecked="{Binding EnablePostProcessing}" VerticalAlignment="Center" Margin="0,0,20,0" ToolTip="调用C++非托管库提取特征并保存数据集。"/>
                </StackPanel>

                <StackPanel Orientation="Horizontal">
                    <Button Content="加载 CSV 列表" Command="{Binding LoadCsvCommand}" Width="120" Margin="0,0,20,0"/>

                    <TextBlock Text="最大并发 Case 数:" VerticalAlignment="Center" Margin="0,0,5,0"/>
                    <TextBox Text="{Binding MaxParallelCases, UpdateSourceTrigger=PropertyChanged}" Width="50" VerticalContentAlignment="Center" Margin="0,0,20,0"/>

                    <TextBlock Text="LS-DYNA 分配核心数:" VerticalAlignment="Center" Margin="0,0,5,0"/>
                    <TextBox Text="{Binding NcpuPerCase, UpdateSourceTrigger=PropertyChanged}" Width="50" VerticalContentAlignment="Center" Margin="0,0,30,0"/>

                    <TextBlock Text="LS-DYNA 分配内存大小:" VerticalAlignment="Center" Margin="0,0,5,0"/>
                    <TextBox Text="{Binding MemoryPerCase, UpdateSourceTrigger=PropertyChanged}" Width="50" VerticalContentAlignment="Center" Margin="0,0,30,0" ToolTip="格式必须为 400m 的形式"/>

                    <Button Content="开始批处理" Command="{Binding StartCommand}" Width="100" Background="#4CAF50" Foreground="White" FontWeight="Bold" Margin="0,0,10,0"/>
                    <Button Content="停止任务" Command="{Binding StopCommand}" Width="100" Background="#F44336" Foreground="White" FontWeight="Bold"/>
                </StackPanel>
            </StackPanel>
        </GroupBox>

        <DataGrid Grid.Row="1" ItemsSource="{Binding Cases}" AutoGenerateColumns="False" 
                  CanUserAddRows="False" IsReadOnly="True" HeadersVisibility="Column" AlternatingRowBackground="#F5F5F5">
            <DataGrid.Columns>
                <DataGridCheckBoxColumn Header="选择" Binding="{Binding IsSelected, UpdateSourceTrigger=PropertyChanged}" Width="40"/>

                <DataGridTextColumn Header="序号" Binding="{Binding Index}" IsReadOnly="True" Width="40"/>
                <DataGridTextColumn Header="Case ID" Binding="{Binding CaseId}" Width="140"/>
                <DataGridTextColumn Header="几何模型" Binding="{Binding GeomType}" Width="70"/>

                <DataGridTextColumn Header="L" Binding="{Binding L}" Width="45"/>
                <DataGridTextColumn Header="W" Binding="{Binding W}" Width="45"/>
                <DataGridTextColumn Header="H" Binding="{Binding H}" Width="45"/>

                <DataGridTextColumn Header="装药位置" Binding="{Binding PositionType}" Width="80"/>
                <DataGridTextColumn Header="装药量(kg)" Binding="{Binding ChargeMass}" Width="80"/>
                <DataGridTextColumn Header="装药密度(kg/m3)" Binding="{Binding ChargeDensity}" Width="110"/>
                <DataGridTextColumn Header="爆源坐标 (X,Y,Z)" Binding="{Binding AbsPositionDisplay}" Width="140"/>

                <DataGridTemplateColumn Header="运行状态" Width="90">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding Status}" FontWeight="Bold" Padding="5,2" VerticalAlignment="Center">
                                <TextBlock.Style>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="Foreground" Value="Gray"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding Status}" Value="Running">
                                                <Setter Property="Foreground" Value="Blue"/>
                                            </DataTrigger>

                                            <DataTrigger Binding="{Binding Status}" Value="PreProcessed">
                                                <Setter Property="Foreground" Value="DarkCyan"/>
                                            </DataTrigger>

                                            <DataTrigger Binding="{Binding Status}" Value="Simulated">
                                                <Setter Property="Foreground" Value="DarkOrchid"/>
                                            </DataTrigger>

                                            <DataTrigger Binding="{Binding Status}" Value="Success">
                                                <Setter Property="Foreground" Value="Green"/>
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding Status}" Value="Failed">
                                                <Setter Property="Foreground" Value="Red"/>
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding Status}" Value="Canceled">
                                                <Setter Property="Foreground" Value="Orange"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </TextBlock.Style>
                            </TextBlock>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>

                <DataGridTextColumn Header="最后执行时间" Binding="{Binding LastRunTime}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>

        <GridSplitter Grid.Row="2" Height="5" HorizontalAlignment="Stretch" VerticalAlignment="Center" 
                      Background="#DDDDDD" Margin="0,5,0,5" Cursor="SizeNS" ShowsPreview="True"/>

        <GroupBox Grid.Row="3" Header="实时运行日志" Margin="0">
            <ListBox x:Name="LogListBox" ItemsSource="{Binding Logs}" FontFamily="Consolas" BorderThickness="0" Background="#FAFAFA">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal" Margin="0,2">
                            <TextBlock Text="{Binding Timestamp, StringFormat={}[{0:HH:mm:ss}]}" Foreground="#888888" Margin="0,0,8,0"/>
                            <TextBlock Text="{Binding Message}" Foreground="{Binding DisplayColor}" TextWrapping="Wrap"/>
                        </StackPanel>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </GroupBox>
    </Grid>
</Window>