<Window x:Class="DynaOrchestrator.Desktop.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="DynaOrchestrator 控制台" Height="850" Width="1250"
        WindowStartupLocation="CenterScreen"
        Background="#F3F5F9"
        FontFamily="Segoe UI, Microsoft YaHei, sans-serif">

    <Window.Resources>
        <Style TargetType="TextBox">
            <Setter Property="Padding" Value="6,4"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="BorderBrush" Value="#D1D5DB"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Background" Value="White"/>
        </Style>

        <Style TargetType="Button">
            <Setter Property="Padding" Value="12,6"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Background" Value="#E5E7EB"/>
            <Setter Property="Foreground" Value="#374151"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="6" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#D1D5DB"/>
                </Trigger>
                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Opacity" Value="0.5"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="StartButtonStyle" TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
            <Setter Property="Background" Value="#10B981"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#059669"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="StopButtonStyle" TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
            <Setter Property="Background" Value="#EF4444"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#DC2626"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="DataGrid">
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#E5E7EB"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="RowHeight" Value="32"/>
            <Setter Property="ColumnHeaderHeight" Value="36"/>
            <Setter Property="GridLinesVisibility" Value="Horizontal"/>
            <Setter Property="HorizontalGridLinesBrush" Value="#F3F4F6"/>
            <Setter Property="VerticalGridLinesBrush" Value="Transparent"/>
        </Style>
        <Style TargetType="DataGridColumnHeader">
            <Setter Property="Background" Value="#F9FAFB"/>
            <Setter Property="Foreground" Value="#4B5563"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Padding" Value="8,0"/>
            <Setter Property="BorderThickness" Value="0,0,0,1"/>
            <Setter Property="BorderBrush" Value="#E5E7EB"/>
        </Style>
        <Style TargetType="DataGridCell">
            <Setter Property="Padding" Value="8,0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="DataGridCell">
                        <Border Padding="{TemplateBinding Padding}" Background="Transparent">
                            <ContentPresenter VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="#EFF6FF"/>
                    <Setter Property="Foreground" Value="#111827"/>
                    <Setter Property="BorderThickness" Value="0"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="250"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="White" CornerRadius="8" Padding="15" Margin="0,0,0,15">
            <Border.Effect>
                <DropShadowEffect Color="#000000" Opacity="0.05" BlurRadius="10" ShadowDepth="2"/>
            </Border.Effect>

            <StackPanel Orientation="Vertical">
                <TextBlock Text="任务控制与全局配置" FontWeight="Bold" FontSize="16" Foreground="#111827" Margin="0,0,0,15"/>

                <StackPanel Orientation="Horizontal" Margin="0,0,0,12">
                    <TextBlock Text="工作区目录 (RootDir):" VerticalAlignment="Center" Margin="0,0,10,0" Foreground="#4B5563"/>
                    <TextBox Text="{Binding WorkspaceRootDir, UpdateSourceTrigger=PropertyChanged}" Width="200" Margin="0,0,25,0" ToolTip="相对于程序运行目录"/>

                    <TextBlock Text="CSV 路径 (CasesCsv):" VerticalAlignment="Center" Margin="0,0,10,0" Foreground="#4B5563"/>
                    <TextBox Text="{Binding CasesCsvPath, UpdateSourceTrigger=PropertyChanged}" Width="250" Margin="0,0,25,0" ToolTip="相对于工作区目录"/>
                </StackPanel>

                <StackPanel Orientation="Horizontal" Margin="0,0,0,15">
                    <TextBlock Text="执行阶段开关:" VerticalAlignment="Center" FontWeight="SemiBold" Foreground="#374151" Margin="0,0,15,0"/>
                    <CheckBox Content="1. 前处理 (网格生成)" IsChecked="{Binding EnablePreProcessing}" VerticalAlignment="Center" Margin="0,0,25,0" ToolTip="取消勾选将跳过基于STL生成网格的过程，直接使用已有K文件。"/>
                    <CheckBox Content="2. 求解器 (LS-DYNA)" IsChecked="{Binding EnableSimulation}" VerticalAlignment="Center" Margin="0,0,25,0" ToolTip="取消勾选将不启动DYNA计算。"/>
                    <CheckBox Content="3. 后处理 (特征提取与NPZ)" IsChecked="{Binding EnablePostProcessing}" VerticalAlignment="Center" Margin="0,0,25,0" ToolTip="调用C++非托管库提取特征并保存数据集。"/>
                </StackPanel>

                <Rectangle Height="1" Fill="#E5E7EB" Margin="0,0,0,15"/>

                <StackPanel Orientation="Horizontal">
                    <Button Content="加载 CSV 列表" Command="{Binding LoadCsvCommand}" Width="120" Margin="0,0,15,0"/>
                    <Button Content="全选" Command="{Binding SelectAllCommand}" Width="60" Margin="0,0,10,0"/>
                    <Button Content="取消全选" Command="{Binding DeselectAllCommand}" Width="80" Margin="0,0,30,0"/>

                    <TextBlock Text="最大并发 Case 数:" VerticalAlignment="Center" Margin="0,0,10,0" Foreground="#4B5563"/>
                    <TextBox Text="{Binding MaxParallelCases, UpdateSourceTrigger=PropertyChanged}" Width="50" Margin="0,0,25,0"/>

                    <TextBlock Text="LS-DYNA 分配核心数:" VerticalAlignment="Center" Margin="0,0,10,0" Foreground="#4B5563"/>
                    <TextBox Text="{Binding NcpuPerCase, UpdateSourceTrigger=PropertyChanged}" Width="50" Margin="0,0,25,0"/>

                    <TextBlock Text="LS-DYNA 分配内存:" VerticalAlignment="Center" Margin="0,0,10,0" Foreground="#4B5563"/>
                    <TextBox Text="{Binding MemoryPerCase, UpdateSourceTrigger=PropertyChanged}" Width="60" Margin="0,0,30,0" ToolTip="格式必须为 400m 的形式"/>

                    <Button Content="开始批处理" Command="{Binding StartCommand}" Style="{StaticResource StartButtonStyle}" Width="120" Margin="0,0,15,0"/>
                    <Button Content="停止任务" Command="{Binding StopCommand}" Style="{StaticResource StopButtonStyle}" Width="100"/>
                </StackPanel>
            </StackPanel>
        </Border>

        <Border Grid.Row="1" Background="White" CornerRadius="8" Padding="1">
            <Border.Effect>
                <DropShadowEffect Color="#000000" Opacity="0.05" BlurRadius="10" ShadowDepth="2"/>
            </Border.Effect>
            <DataGrid ItemsSource="{Binding Cases}" AutoGenerateColumns="False" 
                      CanUserAddRows="False" IsReadOnly="True" HeadersVisibility="Column" 
                      AlternatingRowBackground="#FAFAFA" BorderThickness="0">
                <DataGrid.Columns>
                    <DataGridTemplateColumn Header="选择" Width="60">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <CheckBox IsChecked="{Binding IsSelected, UpdateSourceTrigger=PropertyChanged}" 
                                          HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>

                    <DataGridTextColumn Header="序号" Binding="{Binding Index}" Width="50"/>
                    <DataGridTextColumn Header="Case ID" Binding="{Binding CaseId}" Width="100"/>
                    <DataGridTextColumn Header="几何模型" Binding="{Binding GeomType}" Width="80"/>

                    <DataGridTextColumn Header="L" Binding="{Binding L}" Width="50"/>
                    <DataGridTextColumn Header="W" Binding="{Binding W}" Width="50"/>
                    <DataGridTextColumn Header="H" Binding="{Binding H}" Width="50"/>

                    <DataGridTextColumn Header="装药位置" Binding="{Binding PositionType}" Width="90"/>
                    <DataGridTextColumn Header="当量(kg)" Binding="{Binding ChargeMass}" Width="80"/>
                    <DataGridTextColumn Header="密度(kg/m3)" Binding="{Binding ChargeDensity}" Width="100"/>
                    <DataGridTextColumn Header="爆源坐标 (X,Y,Z)" Binding="{Binding AbsPositionDisplay}" Width="200"/>

                    <DataGridTemplateColumn Header="运行状态" Width="100">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding Status}" FontWeight="Bold" Padding="5,0" VerticalAlignment="Center">
                                    <TextBlock.Style>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="Foreground" Value="#9CA3AF"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding Status}" Value="Running">
                                                    <Setter Property="Foreground" Value="#3B82F6"/>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding Status}" Value="PreProcessed">
                                                    <Setter Property="Foreground" Value="#0891B2"/>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding Status}" Value="Simulated">
                                                    <Setter Property="Foreground" Value="#8B5CF6"/>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding Status}" Value="Success">
                                                    <Setter Property="Foreground" Value="#10B981"/>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding Status}" Value="Failed">
                                                    <Setter Property="Foreground" Value="#EF4444"/>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding Status}" Value="Canceled">
                                                    <Setter Property="Foreground" Value="#F59E0B"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </TextBlock.Style>
                                </TextBlock>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>

                    <DataGridTextColumn Header="最后执行时间" Binding="{Binding LastRunTime}" Width="*"/>
                </DataGrid.Columns>
            </DataGrid>
        </Border>

        <GridSplitter Grid.Row="2" Height="6" HorizontalAlignment="Stretch" VerticalAlignment="Center" 
                      Background="Transparent" Margin="0,5,0,5" Cursor="SizeNS" ShowsPreview="True"/>

        <Border Grid.Row="3" Background="#1E1E1E" CornerRadius="8" Padding="10">
            <Border.Effect>
                <DropShadowEffect Color="#000000" Opacity="0.1" BlurRadius="10" ShadowDepth="2"/>
            </Border.Effect>
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <TextBlock Grid.Row="0" Text="实时运行日志" Foreground="#9CA3AF" FontSize="12" FontWeight="SemiBold" Margin="0,0,0,8"/>

                <ListBox x:Name="LogListBox" Grid.Row="1" ItemsSource="{Binding Logs}" 
                         FontFamily="Consolas" BorderThickness="0" Background="Transparent"
                         ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal" Margin="0,2">
                                <TextBlock Text="{Binding Timestamp, StringFormat={}[{0:HH:mm:ss}]}" Foreground="#6B7280" Margin="0,0,8,0"/>
                                <TextBlock Text="{Binding Message}" Foreground="{Binding DisplayColor}" TextWrapping="Wrap"/>
                            </StackPanel>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                    <ListBox.ItemContainerStyle>
                        <Style TargetType="ListBoxItem">
                            <Setter Property="Focusable" Value="False"/>
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="ListBoxItem">
                                        <ContentPresenter/>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </ListBox.ItemContainerStyle>
                </ListBox>
            </Grid>
        </Border>
    </Grid>
</Window>